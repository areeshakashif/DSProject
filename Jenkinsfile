pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                // Define build steps here
                echo 'Building the code...'
            }
        }
    }
}
